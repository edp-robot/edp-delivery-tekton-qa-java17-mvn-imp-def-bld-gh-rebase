apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "java17-mvn-imp-def-bld-gh-rebase.fullname" . }}-test-connection"
  labels:
    {{- include "java17-mvn-imp-def-bld-gh-rebase.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "java17-mvn-imp-def-bld-gh-rebase.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
